import {test, expect} from '@playwright/test';
test('Test case: Register new account', async ({page}) => {

await test.step('Step 1: Go to the register page', async () => {
    //await page.goto(data.urls.registerPage);
});

}); 